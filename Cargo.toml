[package]
name = "git-link"
version = "0.1.1"
edition = "2024"
description = "`git link` alias to fetch a link to a remote git repo"
license = "Unlicense"

[dependencies]
clap = { version = "4.5.54", features = ["derive"] }
